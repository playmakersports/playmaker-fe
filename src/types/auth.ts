export type AuthResponseType = {
  access_token: string;
  expires_in: number;
  id_token: string;
  newUserYn: string;
  refresh_token: string;
  refresh_token_expires_in: number;
  scope: string;
  token_type: string;
};
